# Validación

- `node --check process-manage-class.js`: correcto.
- `git diff --check`: correcto.
- OpenSpec estricto: válido.
- Graphify `--code-only`: confirmó que `command_card()` llama a
  `item_action_class()` al renderizar el botón individual.
- `bench build --app restaurant_management`: correcto.
- Caché de `v15.local` limpiada y servidor local disponible.
